* Improve filter regexs
* Have findBruteSSH use the regexs from filter.d/blacklist.conf rather than its
own static ones.
* Debug the initial startup problem
* Improve then explain the -auto jail's operation
